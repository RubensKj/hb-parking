language: java
install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:
  - mvn test
services:
  - docker
before_install:
  - cd docker
  - docker run -d -p 192.168.33.214:1433
  - docker ps -a